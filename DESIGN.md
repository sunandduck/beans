# DESIGN.md

## 产品定位
拼豆图纸生成器 — 将照片/图片转化为可直接用于拼豆手工的像素化图纸。面向手工爱好者、DIY 玩家。

## 气质与意象
像素手工工作坊的温暖感。想象一张浅色木质工作台，上面散落着五颜六色的拼豆，阳光从窗户洒进来。界面融合像素艺术风格与手作温度 — 用方块元素、像素字体和阶梯状边框传递拼豆文化的独特美感，同时保持温暖友好的色调。

## 配色方案
- **背景**：温暖奶油白 `#FAF8F5` — 像铺了亚麻布的工作台
- **主色**：珊瑚橙 `#E8734A` — 像拼豆中最常用的暖橙色调
- **主色悬停**：深珊瑚 `#D4623B`
- **辅助色**：薄荷绿 `#7BC8B0` — 清新点缀
- **强调色**：柔和蓝 `#6BA3D6` — 信息提示
- **文字主色**：炭灰 `#2D2A26` — 温暖的黑
- **文字次色**：暖灰 `#7A756E`
- **卡片背景**：纯白 `#FFFFFF`
- **边框**：暖灰线 `#E8E4DF`

## 字体排版
- **标题/装饰**：像素字体 "Press Start 2P"（Google Fonts），用于标题、按钮文字、标签等装饰性文字
- **正文**：系统默认无衬线体，保证可读性
- **数字/代码**：等宽字体，用于网格尺寸等

## 像素化UI元素
- **按钮**：使用阶梯状边框（box-shadow模拟像素阶梯），无圆角
- **卡片**：使用硬边框 + 像素风阴影（offset shadow，无blur）
- **图标**：使用lucide图标，但通过CSS添加像素化滤镜
- **装饰**：添加像素风小元素（如角落的小方块装饰）
- **边框**：使用 `image-rendering: pixelated` 和阶梯状border

## 交互与动效
- **悬停效果**：使用阶梯状位移（transform: translate(-2px, -2px)）+ 阴影变化，模拟像素按钮按下效果
- **过渡**：使用 `steps()` 缓动函数代替 smooth ease，营造逐帧动画感
- **加载动画**：像素化闪烁效果
- **按钮点击**：向下位移 + 阴影缩小，模拟物理按压

## 设计禁忌
- 不要平滑的圆角（用直角或阶梯状替代）
- 不要模糊的阴影（用硬边offset shadow替代）
- 不要渐变（用纯色块替代）
- 不要深色模式
- 不要让UI抢了图纸的风头 — 像素风格是点缀，不是主角
