# REG.md - 项目重要结论记录

> 本文档记录项目中的关键结论和约束，避免后续开发中出现前后矛盾或重复踩坑。
> **每次思考前必须查阅本文档，确保不违反已有结论。**

---

## 1. 拼豆图纸设计原则

### 核心风格
- **Q版大头小身体（chibi 风格）**，专为拼豆手工制作设计

### 设计原则（拼豆可拼性 > 还原度）
- 只抓核心要素：脸型轮廓、发型、眼睛、嘴巴、主要服装色块
- 忽略细节：不要毛孔、皱纹、衣物纹理、配饰细节
- 每个特征用 1-2 个纯色块表示，不要渐变

### 构图要求
- 大头小身体：头部占画面 50-60%，身体占 20-30%
- 头部简化为圆形或椭圆形，不要复杂脸型
- 眼睛：大而简单，2 个黑色圆点或椭圆，占头部 1/4
- 嘴巴：一条简单弧线或省略
- 发型：用 1-2 个大色块概括，不要发丝细节
- 服装：用 1-2 个纯色块，不要图案和纹理

### 技术要求
- 透明背景（alpha 通道为 0），不要任何背景色
- 粗黑色轮廓线（2-3 像素宽）勾勒主体
- 绝对纯色块，无渐变、无抗锯齿、无阴影
- 颜色数量限制在 4-8 种（越少越好）
- 主体居中，占画面 70-80%
- 每个色块至少 8×8 像素，不要有小于 8×8 的细节
- 整体效果应该像一个 32×32 的像素画

### 禁止事项
- 禁止写实风格、照片质感
- 禁止小细节（小于 8 像素的元素）
- 禁止渐变、模糊、抗锯齿、阴影
- 禁止复杂纹理、图案、装饰
- 禁止超过 8 种颜色
- 禁止复杂背景

### 参考风格
Q版表情包、chibi 贴纸、简单像素头像

---

## 2. COZE SDK 限制

### 域名限制
- **COZE SDK 只能在 `coze.site` 域名下调用**
- 无法在自有服务器、自有域名上使用
- 这是 COZE SDK 的硬性限制，无法绕过

### 历史决策
- 项目已从 COZE SDK 迁移到火山引擎 API 直接调用
- 原因：需要部署到自有服务器/Netlify/Vercel 等平台
- 当前使用火山方舟 API（Bearer Token 鉴权），无域名限制

---

## 3. Vercel 部署限制

### 中国大陆访问问题
- **Vercel 在中国大陆无法正常访问，需要翻墙**
- 这是 Vercel 平台的网络限制，无法通过配置解决
- 如果目标用户在中国大陆，不应选择 Vercel 作为部署平台

### 历史测试结论
- 已在实际测试中验证 Vercel 国内访问受限
- 此结论为最终结论，无需重复验证

---

## 4. 部署平台选择指南

| 平台 | 中国大陆访问 | 超时限制 | 成本 | 推荐场景 |
|-----|-------------|---------|------|---------|
| **Netlify** | ✅ 可访问 | 免费版 30 秒 | 免费/$19/月(Pro) | 快速原型、海外用户 |
| **Vercel** | ❌ 需翻墙 | 免费版 60 秒 | 免费 | 海外用户、Next.js 原生支持 |
| **自有服务器** | ✅ 可访问 | 无限制 | ~¥50/月 | 生产环境、国内用户 |
| **云函数** | ✅ 可访问 | 15 分钟 | 按量付费 | 低成本、弹性伸缩 |

### 当前项目选择
- **开发/测试**：Netlify（快速部署）
- **生产环境**：待用户决定（自有服务器备案中 / 云函数）

---

## 5. AI 模型配置

### 当前使用模型
- **模型**：`doubao-seedream-5-0-260128`（Seedream 5.0 Lite）
- **价格**：0.22 元/张
- **输出尺寸**：2048×2048
- **API 类型**：图生图（传入原图作为参考）

### 模型对比（已验证）
| 模型 | 价格 | 特点 |
|-----|------|------|
| Seedream 4.0 | 0.20 元/张 | 最便宜 |
| Seedream 5.0 Lite | 0.22 元/张 | 性价比，支持联网搜索 |
| Seedream 5.0 Pro | 0.30 元/张 | 画质最好，但贵 |

---

## 6. 其他重要结论

### 抠图功能状态
- `/api/remove-bg` 接口存在但**前端未集成**
- 依赖腾讯云 COS + 数据万象（已禁用）
- 如需启用，需配置腾讯云环境变量

### 前端使用的引擎
- 前端实际使用 `perler-engine-simple.ts`（不是 `perler-engine.ts`）
- 修改图纸生成逻辑时注意区分

### 环境变量
- **必需**：`ARK_API_KEY`（火山方舟 API Key）
- **可选（已禁用）**：`TENCENTCLOUD_SECRET_ID`、`TENCENTCLOUD_SECRET_KEY`、`COS_BUCKET`、`COS_REGION`

---

## 更新记录

| 日期 | 更新内容 |
|-----|---------|
| 2025-07-26 | 初始创建，记录拼豆设计原则、COZE SDK 限制、Vercel 访问限制 |
